# agy lane A — Report + vision

W:\Teja\7_Hrs_Project SEAL Next.js 14. Edit ONLY:
- app/report/page.tsx
- app/api/vision/route.ts

Do not edit map, Command, Queue, Shell, globals, rank, weather, gee, analytics, crew, ledger, or git.

1. Sample buttons call analyze(null, filename). Blocked → clogClass blocked / clog 92. Plastic 86. Silt 78. Clear 12.
2. Overlay JSON (class + clog) on camera. Never blank if fetch fails.
3. Vision: Gemini 2.0 Flash if GEMINI_API_KEY else filename heuristic. Never crash without a key.
4. No Mapbox, chatbot, Landsat, Twilio. Do not commit.

Print a short summary of files changed. Skip npm run build if shell is denied.

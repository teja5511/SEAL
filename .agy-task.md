Complete the SEAL (Storm Emergency Action Ledger) Next.js 14 app in W:\Teja\7_Hrs_Project. Dependencies are already installed. Do NOT stop until the app builds and `npm run dev` can serve all four screens.

PRODUCT
SEAL: seal clogged storm drains before rain. Pay waste-picker crews only on verified before/after photos. Dark ops UI (ink #07080a, teal #2ee6c5, amber #ffb020). City-agnostic brand; Hyderabad nala seed data.

ALREADY BUILT — do not rewrite unless broken:
- package.json, next.config.mjs, tsconfig.json, tailwind.config.ts, postcss.config.mjs
- data/hyderabad-nalas.json (24 pins)
- lib/types.ts, lib/wbgt.ts, lib/rank.ts, lib/scenario.ts, lib/store.ts

YOU MUST CREATE

1) app/globals.css — Tailwind layers, dark cinematic styles, radar sweep keyframes, maplibre overrides.
2) app/layout.tsx — Syne + Geist via next/font/google (Syne, Geist if available else Inter + JetBrains Mono). Shell nav: SEAL / Command / Report / Crew / Ledger. Tagline: "Seal the drain before the rain." Show data-source chip.
3) app/page.tsx — COMMAND CENTER (70% of demo):
   - Full-viewport MapLibre GL dark streets (https://basemaps.cartocdn.com/gl/dark-matter-gl-style/style.json)
   - Ranked nala queue panel, T-minus clock, Replay Storm button that steps -6h → -4h → -2h → 0
   - Pins colored WATCH/YELLOW/RED; dispatched/verified/held status
   - WeatherNext 3 p90 precip + WBGT crew chip (WORK / SHADE_BREAK / HOLD)
   - Split view at T-0: unsealed ward floods vs sealed ward does not
   - Live weather toggle calling /api/weather (fallback Open-Meteo if no GEE key)
4) app/report/page.tsx — Camera/upload drain photo. POST /api/vision. Overlay JSON: clear|silt|plastic|blocked + 0-100 clog. If no GEMINI_API_KEY, heuristic fallback so demo never blanks.
5) app/crew/page.tsx — Pixel-perfect WhatsApp-looking phone UI. Job card: pin, ₹, heat HOLD if WBGT>=32, after-photo. Simulated chat messages in English + Telugu.
6) app/ledger/page.tsx — Before/after slider, ₹ paid only on verified jobs, kg plastic, households not inundated. Counterfactual map summary.
7) components: MapView, Queue, ReplayClock, WbgtChip, PhoneFrame, ProofSlider, Shell, SourceBadge. Client components where needed ('use client').
8) app/api/vision/route.ts — Gemini 2.0 Flash structured JSON if GEMINI_API_KEY else filename heuristic.
9) app/api/weather/route.ts — Try Earth Engine WeatherNext 3 (projects/gcp-public-data-weathernext/assets/weathernext_3_0_0_0p1deg bands total_precipitation_1hr_mean/p90, temperature_2m_mean; 0.05deg station_head_temperature_2m_mean / dewpoint). If EE_PRIVATE_KEY_JSON or EE_PROJECT missing, Open-Meteo for Hyderabad 17.385,78.486. Return source field: weathernext3 | open-meteo | replay.
10) lib/gee.ts — Node-only @google/earthengine authenticateViaPrivateKey if env present. Dynamic World water, Sentinel-2 NDWI/NDBI, SRTM elevation, WeatherNext 3 precip p90. Never crash without keys — return gee-cache.
11) data/gee-cache.json — plausible Hyderabad telemetry so Replay Storm works offline.
12) public/demo/*.svg — clogged/clear drain illustrations for before/after.
13) .env.example — GEMINI_API_KEY, EE_PROJECT, EE_PRIVATE_KEY_JSON, NEXT_PUBLIC_MAP_STYLE
14) README.md + DEVPOST.md — video beat sheet, Earth Forward story, dual-core (deterministic rank, LLM cannot change score), WeatherNext allowlist note (5-7 days; Open-Meteo fallback).

RULES
- TypeScript strict. No chatbot. No Mapbox. No Landsat sidewalk routing.
- Dual-core: ranking in lib/rank.ts is immutable; Gemini only explains.
- App must `npx tsc --noEmit` clean enough to run next dev.
- Use react-map-gl/maplibre or maplibre-gl in a client component. dynamic import ssr:false for the map.
- Keep files focused. Beautiful, video-native, large type, motion via framer-motion.
- When done: run npm run build and fix errors.

Do the work. Do not only describe it.

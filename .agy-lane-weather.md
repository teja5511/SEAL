# agy lane C — Weather + Earth Engine

W:\Teja\7_Hrs_Project SEAL Next.js 14. Edit ONLY:
- app/api/weather/route.ts
- lib/gee.ts

Do not edit map, Command, UI pages, report, vision, analytics, DEVPOST, rank, or git.

1. If EE_PROJECT / EE_PRIVATE_KEY_JSON missing, never throw. Fallback Open-Meteo (Hyderabad 17.385,78.486) then gee-cache/replay.
2. Return source: weathernext3 | open-meteo | replay. Keep existing JSON shape (precip, temp, dew, humidity, wind, WBGT inputs).
3. WeatherNext 3 asset only if keys exist. Node-only @google/earthengine.
4. No Mapbox, chatbot, Landsat sidewalk routing.

Print a short summary. Skip npm run build if shell is denied.

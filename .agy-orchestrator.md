# SEAL agent board — orchestrator owns agy

Updated: 2026-09-21 00:18 IST  
App: http://localhost:3000 (all four screens + `/api/weather` 200)  
This file is the cross-chat handoff. Cursor chats cannot ping each other; read this before editing.

## Roles

| Chat | Job | Files |
|---|---|---|
| **Antigravity agent coordination** (this chat) | Keep `agy` terminals healthy. Do **not** build product UI. | `.agy-*.md` only |
| **AI sustainability hackathon idea** | Build SEAL Command + Report | `app/page.tsx`, `app/report/**`, `components/{MapView,ReplayClock,Queue,DynamicMap,WbgtChip,SourceBadge}.*` |
| **Seal project information** | Build SEAL Crew + Ledger | `app/crew/**`, `app/ledger/**`, `components/{PhoneFrame,ProofSlider}.*` |

Shared libs (`lib/*`, `app/api/*`, `app/layout.tsx`, `app/globals.css`) — one editor at a time. Announce here before touching them.

## agy status — IDLE (do not relaunch)

Previous Gemini 3.8 Flash High jobs **succeeded and exited**. Do not start a new `agy` process unless the orchestrator writes `agy: RELAUNCH` in this file. Two agy jobs already collided on Command files once.

| Terminal | Result |
|---|---|
| First `agy` (full `.agy-task.md`) | succeeded ~10 min, then `npm run build` |
| Remaining punch list | succeeded, 0 build errors |
| Command Replay Storm only | succeeded, tsc + build green |
| Two early PowerShell launches | **failed** (`unexpected argument "the"`) — prompt was split into argv |

**Launch rule (orchestrator only):** one `-p` string, never splat a multiline prompt in PowerShell.

```powershell
agy --model gemini-3.8-flash-high --effort high --mode accept-edits --add-dir "W:\Teja\7_Hrs_Project" --dangerously-skip-permissions --output-format text -p "Read W:\Teja\7_Hrs_Project\.agy-remaining.md and execute it. Edit files. Stop when npm run build succeeds."
```

Antigravity IDE processes are running (black/white apps). CLI binary: `C:\Users\Pavan Teja\AppData\Local\agy\bin\agy.exe`.

## Runtime

- `npm run dev` is **up**. A brief `/` and `/report` timeout earlier recovered; all routes 200 again.
- GitHub auth for any later `git push`: GCM user **`teja5511`**. No `agy` GitHub login. Waiting on user for commit name/email before add/commit.
- Stale `.next` cache already cleared once. Do not kill the server unless it 500s again.

## Builder punch list (product work — not agy)

Do **not** rewrite the Command map from scratch.

1. Replay Storm T–6 → T–4 → T–2 → T–0 must change hour **and** `buildState`. T–0 = split view, RED flood auras, teal sealed shields.
2. `/report` sample **blocked** must analyze as blocked / ~92, not plastic / 86. Pass filename into `analyze()`.
3. `/crew` after-photo verifies dispatched jobs; HeatGuard HOLD stays unpaid; Telugu lines stay visible.
4. `/ledger` before/after slider + ₹ only on verified proofs at hour 0.
5. Keep Carto dark-matter tiles. No Mapbox, chatbot, Landsat sidewalks, Twilio.
6. `npm run build` must stay green.

## Collision protocol

- If you see another chat editing your lane, stop and write a line under **Log**.
- Orchestrator will only spawn `agy` for a **scoped** file list that does not overlap a live builder.
- Heartbeat cadence: 15 minutes while idle.

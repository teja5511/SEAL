# agy quality lane — execute now

You are Gemini in W:\Teja\7_Hrs_Project (SEAL Next.js 14). Other Cursor chats own the Command map. Stay out of their files.

## DO NOT EDIT (live in other chats)
- app/page.tsx
- components/MapView.tsx
- components/DynamicMap.tsx
- components/Queue.tsx
- components/ReplayClock.tsx
- components/Shell.tsx
- lib/map-style.ts
- app/globals.css
- app/layout.tsx
- lib/rank.ts (immutable Core 1)
- Do not git commit, push, or change git config
- No Mapbox. No chatbot. No Landsat sidewalks. No Twilio.

## EDIT ONLY
- app/report/page.tsx
- app/api/vision/route.ts
- app/analytics/page.tsx
- app/api/weather/route.ts
- lib/gee.ts
- DEVPOST.md

## TASKS (quality, do not rewrite from scratch)

1. Report `/report`: sample buttons must call analyze(null, filename). **Blocked** → clogClass blocked / clog 92. Plastic stays 86. Overlay the JSON (class + clog) so a judge can read it on camera. Empty/error state if fetch fails — never blank.

2. Vision API: heuristic must key off filename (`block` → 92, `silt` → 78, `clear` → 12, else plastic 86). Gemini 2.0 Flash if GEMINI_API_KEY else heuristic. Never crash without a key.

3. Analytics `/analytics`: video-ready scoreboard that **reads** buildState / ledgerStats only. Gemini cannot invent risk. EN copy (storm drain, not nala unless using existing LocaleContext). Empty-safe if arrays empty.

4. Weather `/api/weather` + `lib/gee.ts`: if EE keys missing, never throw — Open-Meteo then gee-cache/replay. Return `source`: weathernext3 | open-meteo | replay. Keep existing return shape.

5. DEVPOST.md: add Analytics to the beat sheet; EN is default, EN+TE optional; pay only on verified after-photo; dual-core (rank immutable). Do not mention Carto if the live map is OpenFreeMap — do not change map code.

When done: print a short summary of files changed. Do not run git. You may skip `npm run build` if shell is denied.

Builders: execute this. Orchestrator owns `agy` — do not launch agy from this file.

Finish SEAL polish in W:\Teja\7_Hrs_Project. App already builds. Dev server: http://localhost:3000

Read `.agy-orchestrator.md` first and stay in your assigned lane.

FIXES (only if still broken — do not rewrite Command map from scratch)
1. app/report/page.tsx: sample buttons must pass filename into analyze() so "blocked" returns clogClass blocked / 92, not plastic / 86.
2. Replay Storm T-0 must flip split view, RED flood auras, and ledger-linked verified pins. Clicking T–4 / T–2 / T–0 must update hour AND appState (buildState).
3. app/crew/page.tsx: after-photo on a dispatched job verifies; HOLD job stays unpaid. Telugu lines stay visible.
4. app/ledger/page.tsx: before/after slider + rupee counters from verified proofs at hour 0.
5. If map tiles fail, keep Carto dark-matter style; never add Mapbox.

THEN
- npm run build must stay green.
- Polish empty states only if broken.
- Do not add a chatbot, Landsat sidewalks, or Twilio.

When done, print a short summary of files changed.
